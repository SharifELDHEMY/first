on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name:" testing git hub action "
        run: pwd

        
      - name:" print "
        run: echo hello